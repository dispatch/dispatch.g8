name :=  "$name$"

scalaVersion := "2.12.3"

libraryDependencies += "org.dispatchhttp" %% "dispatch-core" % "$version$"

resolvers += ("Sonatype latest" at "https://oss.sonatype.org/service/local/repositories/releases/content/")
